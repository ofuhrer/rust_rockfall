# Reference Notes Archive

Older standalone notes live here so `docs/` stays focused on the current
working interface.

Do not add new notes here by default. Prefer deleting obsolete notes or folding
durable facts into the current docs.
