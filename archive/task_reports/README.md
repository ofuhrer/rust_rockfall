# Task Report Archive

Historical TB-specific reports live here so `docs/` can stay focused on the
current working interface for humans and agents.

Use these files only when a current task needs the original detailed evidence
behind a summarized result. Prefer updating the current docs over adding new
standalone task reports.
