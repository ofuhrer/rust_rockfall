# Validation Data

Use this directory for validation-ready files derived from public datasets when those files are small enough and license-compatible for version control.

Large or raw downloads belong under `data/raw/<dataset_id>/` and remain ignored by git. Derived files should be reproducible from `scripts/preprocess_datasets.py` and documented in `data/datasets.yaml`.

