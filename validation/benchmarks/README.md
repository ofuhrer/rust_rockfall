# Public Benchmark Scaffolds

Tracked files under this directory describe public benchmark package contracts.
Large public raw archives, generated cases, validation outputs, QA plots, and
hazard products stay ignored under `data/raw/`, `validation/results/`, and
`hazard/results/`.

See `docs/public_benchmark_framework.md` for the scientific roles and
preparation-manifest contract.
